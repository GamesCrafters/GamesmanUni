  
name: test
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Pushing build to NYC
        uses: fifsky/ssh-action@master
        with:
          command: |
            ls -a
          host: nyc.cs.berkeley.edu
          user: uni
          pass: ${{ secrets.KEY }}

  
name: test
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Pushing build to NYC
        uses: fifsky/ssh-action@master
        with:
          command: |
            cd /home/uni
            touch helloworld
            ls -a
          host: nyc.cs.berkeley.edu
          user: github
          key: ${{ secrets.KEY }}

<template>
    <AppGame />
</template>

<script lang="ts" setup>
    // import { computed } from "vue";
    // import { useMeta } from "vue-meta";
    // import { useRoute } from "vue-router";
    // import { useStore } from "../../scripts/plugins/store";
    import AppGame from "../units/AppGame.vue";

    // const route = useRoute();
    // const store = useStore();
    // const game = computed(() => store.state.app.gameTypes[<string>route.params.type].games[<string>route.params.gameId]);
    // const gameName = computed(() => game.value.name);
    // const variantDescription = computed(() => game.value.variants.variants[<string>route.params.variantId].description);
    // useMeta(computed(() => ({ title: `${variantDescription.value} ${gameName.value}` })));
</script>
